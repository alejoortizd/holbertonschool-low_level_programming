#include "holberton.h"
#include <string.h>
/**
 * _strlen - Entry point
 * Description: function that return the length of a string
 * @s: parametre to check
 * Return: n;
 */
int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != 0; n++)
	{
	}
	return (n);
}
