#include "holberton.h"
/**
 * factorial - Entry point
 * Description: function thar return the n factorial
 * @n: Parametre to check
 * Return: n * factorial (n-1)
 */
int factorial(int n)
{
	if (n < 0)
		return (-1);
	if (n < 1)
		return (1);
	return (n * factorial(n-1));
}
