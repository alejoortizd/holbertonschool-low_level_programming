#include "holberton.h"
/**
 * _isalpha - check the code for Holberton School students.
 *
 * @c: is a parametre to check
 *
 * Return: Always 0.
 */
int _isalpha(int c)
{
	if (c == 0)
		return (0);
	else
		return (1);
	_putchar('\n');
}
