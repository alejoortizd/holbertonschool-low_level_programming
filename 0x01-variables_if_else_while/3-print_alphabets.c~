#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

int a = 96;
int b = 64;
while (a < 122)
{
a++;
putchar(a);
}
while ( b < 90)
{
b++;
putchar(b);
}
putchar('\n');
return (0);
}
